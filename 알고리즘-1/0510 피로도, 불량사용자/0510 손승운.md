## 0507

<br>

<br>

#### 피로도

```python
visit = []

def solution(k, dungeons, n):
    if len(dungeons) == n:
        return
    else:
        for i in range(len(dungeons)):
            if dungeons[i][]
            .append(dungeons)
    return answer


def solution(k, dungeons):
    life = [0]
    for i in range(len(dungeons)):
        c = []
        for j in life:
            if
            c.append(k - dungeons[i])
        life = c
    return
```

<br>

<br>

#### 불량 사용자

```python
def solution(user_id, banned_id):
    while user_id:
        cnt = 0
        if len(banned_id) != len(user_id):
            continue
        elif len(banned_id) == len(user_id):
            for k in range(len(banned_id)):
                if banned_id[k] == user_id[k] or banned_id == '*':
                    cnt += 1
            if cnt == len(banned_id)
               def counting
def counting():
    
```

<br>

---

*end*